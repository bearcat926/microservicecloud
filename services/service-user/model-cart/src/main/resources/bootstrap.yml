spring:
  cloud:
    config:
      name: springcloud-config-service #需要从github上读取的资源名称，注意没有yml后缀名
      #profile配置是什么就取什么配置 dev or test
      profile: dev
      #profile: test
      label: master
      uri: http://127.0.0.1:3344  #SpringCloudConfig获取的服务地址

